<div class="col-12">
  <div class="table-responsive">
    <div class="brand">All Groups</div>
      <div class="address-bar">
        <p>
        <p>
        <%= link_to "Create New Group", new_group_path, class: 'btn btn-default' %>
      </div>
      <table class="table table-hover">

        <tr>
          <th> Group Name </th>
          <th> Description </th>
          <th> Image </th>
          <th> Actions </th>
        </tr>

    <% @groups.each do |group| %>
    <tr>
      <td><%= group.name %></td>
      <td><%= group.description %></td>
      <td><%= image_tag(group.image_url, class: 'img-thumbnail img-responsive') %></td>
      <td>
        <%=link_to 'New Message', new_group_message_path(group), class: 'btn btn-success btn-xs' %>
        <%=link_to 'All Contacts', group_contacts_path(group), class: 'btn btn-warning btn-xs' %>
        <%=link_to 'All Messages', group_messages_path(group), class: 'btn btn-warning btn-xs' %>
        <%=link_to 'Edit Group', edit_group_path(group), class: 'btn btn-warning btn-xs' %>
        <%=link_to 'Delete Group', group, method: :delete, data: {confirm: "Are you Sure?"}, class: 'btn btn-xs btn-danger' %>
      </td>
    </tr>

    <% end %>
  </div>
</div>

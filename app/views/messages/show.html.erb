<% if false %>
      <div class="col-12">
        <div class="table-responsive">
          <h1 style="color:#cc3333;">Message Details</h1>
            <table class="table table-hover">

              <tr>
                <th> Message </th>
                <th> Group </th>
                <th> Sent? </th>
                <th> Scheduled? </th>
                <th> Date sent / To be sent </th>
                <th> Actions </th>
              </tr>

              <tr>
                <td><%= @message.body %></td>
                <td><%= @message.group.name %></td>
                <td><%= @message.sent %></td>
                <td><%= @message.scheduled %></td>
                <td><%= @message.date %></td>
                <td>
                  <%=link_to 'Edit Message', edit_user_group_message_path(@user, @group, @message), class: 'btn btn-warning btn-xs' %>
                  <%=link_to 'Delete Message', user_group_message_path(@user, @group, @message), method: :delete, data: {confirm: "Are you Sure?"}, class: 'btn btn-xs btn-danger' %>
                </td>
              </tr>

        </div>

      <%=link_to "All Group Messages", user_group_messages_path(@user, @group), class: 'btn btn-default btn-lg'%>
      <p>
      </div>
<% end %>


    <div class="brand">Message sent to <br>
    <%= @group.name %></div>

<% send_text_message %>

<p style="color:white">
  <b>Body:</b> <%= @message.body %>
</p>

<div class="address-bar">
  <p>
  <p>
  <%=link_to "All Group Messages", user_group_messages_path(@user, @group), class: 'btn btn-default btn-lg'%>

</div>
